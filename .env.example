# =============================================================================
# Website3.0 – Frontend – Environment Example
# =============================================================================
# Copy to .env and fill values. Safe for Docker, CI/CD, local dev, and production.
# Only VITE_* variables are exposed to the client. Never put secrets here.
# =============================================================================

# -----------------------------------------------------------------------------
# APP
# -----------------------------------------------------------------------------
# Public app title (optional; for meta or UI)
# VITE_APP_TITLE=My App

# -----------------------------------------------------------------------------
# API
# -----------------------------------------------------------------------------
# Base URL of the backend API (Atlas). Required for auth, uploads, and API calls.
# Leave empty for local-only or mock mode. In Docker set via build ARG.
VITE_API_BASE_URL=http://localhost:8000 #dev
#VITE_API_BASE_URL=http://api #prod

# -----------------------------------------------------------------------------
# FEATURE FLAGS (optional)
# -----------------------------------------------------------------------------
# Enable debug or feature flags in the UI (optional)
# VITE_DEBUG=false
# VITE_ENABLE_ANALYTICS=false

# -----------------------------------------------------------------------------
# THIRD PARTY (optional – only if used in frontend)
# -----------------------------------------------------------------------------
# Example: public keys or endpoints for maps, analytics, etc.
# VITE_MAPS_API_KEY=
# VITE_SENTRY_DSN=
