# Optional: set when you have a backend. When unset, contact form shows local success only.
VITE_API_BASE_URL=

# Google Cloud - build-and-deploy-gc.sh
REGION=
REPO=
SERVICE=
IMAGE_NAME=
# TAG opcional; si no se define, se usa fecha-hora (ej: 20250212-143022)
# TAG=
